<div class="text-center">
  <img src="/images/{{book.filename}}">
  <h2>{{title}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{synopsis}}</p>
  <p>Written by {{book.author}}</p>
  <p> Genre: {{genre}}</p>
  <p>{{number_pages}} pages</p>
  <p>First published: {{date_publication}}</p>
  <p>ISBN: {{ISBN}}</p>
  <p>
    <button class="btn btn-primary">Finished this book!</button>
  </p>
</div>