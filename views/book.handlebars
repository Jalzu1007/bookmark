<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Written by {{book.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="money">📚</span>
    {{!-- <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed --}}
  </p>
  <p>
    <button class="btn btn-primary">Finished this book!</button>
  </p>
</div>
